# Security notes

This is a local developer tool, not a magic security boundary.

It helps with provider validation before launch, named network restrictions, project-local configuration, centralized audit logs, and optional `sops-age` secret encryption.
